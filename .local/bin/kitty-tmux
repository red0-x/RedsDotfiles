#!/usr/bin/env zsh
# kitty launcher: start a fresh tmux session per window, fall back to plain zsh
if command -v tmux >/dev/null 2>&1 && [ -z "$TMUX" ]; then
  exec tmux new-session -A -s "w$$"
fi
exec zsh -l
