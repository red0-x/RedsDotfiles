# -----------------------------------------------------
# Powerlevel10k + PATH
# -----------------------------------------------------

# Load p10k theme
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

# npm global
export PATH="$HOME/.npm-global/bin:$PATH"

# spicetify
export PATH=$PATH:/home/red/.spicetify